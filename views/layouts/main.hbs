<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>zzox</title>
    <link rel="stylesheet" href="https://unpkg.com/marx-css/css/marx.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@10.0.0/build/styles/github.min.css">
    <style type="text/css">
      :root {
        --bg-color: #f7f7f2;
        --bg2-color: #e4e6c3;
        --accent-color: #00b000;
        --text-color: #261c15;
        --link-color: #226ce0;
        --code-bg-color: #e8e8e8;
        background-color: var(--bg-color);
        color: var(--text-color);
        {{!-- If dark mode: --}}
        filter: invert(0.9);
      }

      nav ul {
        height: auto;
        width: 100%;
      }

      nav li {
        display: inline;
        color: var(--text-color);
        padding-left: 1em;
        font-size: 1.5em;
        text-shadow: 20px 10px 8px var(--accent-color);
      }

      a {
        color: inherit;
      }

      a:hover {
        text-decoration: none;
        color: inherit;
        cursor: pointer;
      }

      main a {
        color: var(--link-color);
      }

      pre {
        background-color: var(--code-bg-color);
        white-space: pre;
        overflow-x: scroll;
      }

      code {
        white-space: unset;
        font-size: 0.9em;
        overflow-x: scroll;
      }

      .title {
        text-align: center;
        margin-bottom: 0px;
        color: var(--bg-color);
        text-shadow: -2px -2px 0 var(--accent-color), 2px -2px 0 var(--accent-color), -2px 2px 0 var(--accent-color), 2px 2px 0 var(--accent-color);
      }

      .title-main {
        text-align: center;
        margin-bottom: 0px;
        font-size: 64px;
        font-weight: 600;
        color: var(--bg-color);
        text-shadow: -2px -2px 0 var(--accent-color), 2px -2px 0 var(--accent-color), -2px 2px 0 var(--accent-color), 2px 2px 0 var(--accent-color);
      }

      .subtitle-main {
        text-align: center;
        font-weight: 700;
        font-size: 16px;
        color: var(--accent-color);
      }

      .subtitle {
        font-weight: 700;
        margin-top: 25px;
        color: var(--accent-color);
      }

      .clearfix {
        clear: both;
      }

      .created {
        float: right;
      }

      .author {
        float: left
      }

      .card {
        border: 2px #D0D0D0 solid;
        border-radius: 5px;
        margin: 10px 0;
        padding: 10px;
        text-decoration: none;
      }

      .content {
        display: block;
        margin: 0 auto;
        max-width: 768px;
        padding: 0 16px 16px;
      }

      .card-title {
        color: var(--bg-color);
        text-shadow: -1px -1px 0 var(--accent-color), 1px -1px 0 var(--accent-color), -1px 1px 0 var(--accent-color), 1px 1px 0 var(--accent-color);
        margin: 0px;
      }

      .highlight {
        color: var(--accent-color);
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <nav class="navigation">
      <ul>
        <li>
          <a href="/blog">blog</a>
        </li>
        <li>
          <a href="/games">games</a>
        </li>
        <li>
          <a href="/projects">projects</a>
        </li>
      </ul>
    </nav>
    <div class="content">
      {{{body}}}
    </div>
  </body>
</html>
